#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
#include <temp.h>
struct List_<T> {
	struct T (*get)();
	int (*size)();
	/* List_<T>*/ (*add)();
};
struct ");
    }

    private static Optional<String> compileToStruct {
	struct return (*compileInfix)();
};
struct Main {
	struct DivideState (*popAndAppend)();
	struct DivideState (*advance)();
	struct DivideState (*append)();
	/* List_<String>*/ (*segments)();
	int (*isLevel)();
	struct DivideState (*enter)();
	struct DivideState (*exit)();
	int (*isShallow)();
	int (*hasNext)();
	char (*pop)();
	/* List_<String>*/ segments;
	int depth;
	struct StringBuilder buffer;
	struct DivideState (*advance)();
	struct DivideState (*append)();
	struct return this;
	int (*isLevel)();
	struct DivideState (*enter)();
	struct return this;
	struct DivideState (*exit)();
	struct return this;
	int (*isShallow)();
	int (*hasNext)();
	char (*pop)();
	struct DivideState (*popAndAppend)();
	/* List_<String>*/ (*segments)();
	struct record (*Node)();
	/* =*/ (*Files.readString)();
	/* =*/ (*source.resolveSibling)();
	/* Files.writeString(target,*/ compile(input));
	/* }*/ (*catch)();
	struct return (*output.toString)();
	/* }*/ (*if)();
	/* }*/ (*if)();
	struct return (*compileClass)();
	/* );*/ (*}).or)();
};
