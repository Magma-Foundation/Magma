package magma.compile.rule.divide;class package magma.compile.rule.divide;{package magma.compile.rule.divide;

import magma.collect.list.List_;class import magma.collect.list.List_;{

import magma.collect.list.List_;

public interface DividingState {
    DividingState append(char c);

    boolean isLevel();

    DividingState exit();

    DividingState enter();

    DividingState advance();

    List_<String> segments();
}
class public interface DividingState {
    DividingState append(char c);

    boolean isLevel();

    DividingState exit();

    DividingState enter();

    DividingState advance();

    List_<String> segments();
}{

public interface DividingState {
    DividingState append(char c);

    boolean isLevel();

    DividingState exit();

    DividingState enter();

    DividingState advance();

    List_<String> segments();
}
