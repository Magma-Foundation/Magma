package magma.compile.context;class package magma.compile.context;{package magma.compile.context;

import magma.compile.Node;class import magma.compile.Node;{

import magma.compile.Node;

public record NodeContext(Node value) implements Context {
    @Override
    public String display() {
        return value.display();
    }
}class public record NodeContext(Node value) implements Context {
    @Override
    public String display() {
        return value.display();
    }
}{

public record NodeContext(Node value) implements Context {
    @Override
    public String display() {
        return value.display();
    }
}