package magma.compile.context;class package magma.compile.context;{package magma.compile.context;

public interface Context {
    String display();
}
class public interface Context {
    String display();
}{

public interface Context {
    String display();
}
