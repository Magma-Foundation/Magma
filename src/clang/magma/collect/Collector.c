package magma.collect;class package magma.collect;{package magma.collect;

public interface Collector<T, C> {
    C createInitial();

    C fold(C c, T t);
}
class public interface Collector<T, C> {
    C createInitial();

    C fold(C c, T t);
}{

public interface Collector<T, C> {
    C createInitial();

    C fold(C c, T t);
}
