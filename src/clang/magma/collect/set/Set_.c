package magma.collect.set;class package magma.collect.set;{package magma.collect.set;

import magma.collect.stream.Stream;class import magma.collect.stream.Stream;{

import magma.collect.stream.Stream;

public interface Set_<T> {
    Stream<T> stream();

    Set_<T> add(T element);
}
class public interface Set_<T> {
    Stream<T> stream();

    Set_<T> add(T element);
}{

public interface Set_<T> {
    Stream<T> stream();

    Set_<T> add(T element);
}
