// [Actual, Namespace, Collector, EmptyHead, Head, List, ListCollector, Lists, HeadedQuery, Query, RangeHead, SingleHead, Console, IOError, Path, None, Option, Some, Result, Characters, Strings]
export interface Tuple2<A, B> {
	left(): A;
	right(): B;
}
