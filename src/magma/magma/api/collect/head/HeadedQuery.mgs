import magma.api.collect.Query;
import magma.api.collect.head.Head;
import magma.api.option.Option;
import magma.api.collect.Collector;
import magma.api.collect.head.MapHead;
import magma.api.Tuple2;
import magma.api.collect.head.FlatMapHead;
import magma.api.collect.head.EmptyHead;
import magma.api.collect.head.ZipHead;
import magma.api.collect.head.SingleHead;
export struct HeadedQuery<T> implements Query<T> {
	mut head: Head<T>;
	constructor (mut head: Head<T>) {
		this.head = head;
	}
	def next(): Option<T> {
		return this/*auto*/.head.next(/*auto*/);
	}
	def collect<C>(collector: Collector<T, C>): C {
		return this/*auto*/.foldWithInitial(collector/*auto*/.createInitial(/*auto*/), (mut current: C, mut element: T) => collector/*auto*/.fold(current/*auto*/, element/*auto*/));
	}
	def map<R>(mapper: (arg0 : T) => R): Query<R> {
		return HeadedQuery<R>(MapHead<T, R>(this/*auto*/.head, mapper/*auto*/));
	}
	def foldWithInitial<R>(initial: R, folder: (arg0 : R, arg1 : T) => R): R {
		mut let result: R = initial/*auto*/;
		while (true/*auto*/) {
			let finalResult: R = result/*auto*/;
			let maybeNext: Tuple2<Bool, R> = this/*auto*/.head.next(/*auto*/).map((mut inner: T) => folder/*auto*/(finalResult/*auto*/, inner/*auto*/)).toTuple(finalResult/*auto*/);
			if (maybeNext/*auto*/.left(/*auto*/)) {
				result/*auto*/ = maybeNext/*auto*/.right(/*auto*/);
			}
			else {
				return result/*auto*/;
			}
		}
	}
	def foldWithMapper<R>(next: (arg0 : T) => R, folder: (arg0 : R, arg1 : T) => R): Option<R> {
		return this/*auto*/.head.next(/*auto*/).map(next/*auto*/).map((maybeNext: R) => this/*auto*/.foldWithInitial(maybeNext/*auto*/, folder/*auto*/));
	}
	def flatMap<R>(mapper: (arg0 : T) => Query<R>): Query<R> {
		return this/*auto*/.head.next(/*auto*/).map(mapper/*auto*/).map((mut initial: Query<R>) => HeadedQuery<R>(FlatMapHead<T, R>(this/*auto*/.head, initial/*auto*/, mapper/*auto*/))).orElseGet(() => HeadedQuery<R>(EmptyHead<R>(/*auto*/)));
	}
	def allMatch(predicate: (arg0 : T) => boolean): Bool {
		return this/*auto*/.foldWithInitial(true/*auto*/, (mut maybeAllTrue: Bool, mut element: T) => maybeAllTrue/*auto*/ && predicate/*auto*/(element/*auto*/));
	}
	def anyMatch(predicate: (arg0 : T) => boolean): Bool {
		return this/*auto*/.foldWithInitial(false/*auto*/, (mut aBoolean: Bool, mut t: T) => aBoolean/*auto*/ || predicate/*auto*/(t/*auto*/));
	}
	def zip<R>(other: Query<R>): Query<Tuple2<T, R>> {
		return HeadedQuery<Tuple2<T, R>>(ZipHead<T, R>(this/*auto*/.head, other/*auto*/));
	}
	def filter(predicate: (arg0 : T) => boolean): Query<T> {
		return this/*auto*/.flatMap((element: T) => {
			if (predicate/*auto*/(element/*auto*/)) {
				return HeadedQuery<T>(SingleHead<T>(element/*auto*/));
			}
			else {
				return HeadedQuery<T>(EmptyHead<T>(/*auto*/));
			}
		});
	}
}
