// [Lists, Console, Files, Characters, Strings, Actual, Namespace, Collector, EmptyHead]
import magma.api.collect.head.{ Head };
import magma.api.option.{ Option, None };
export struct EmptyHead<T> implements Head<T> {
	def next(): Option<T> {
		return None<T>(/*auto*/);
	}
}
