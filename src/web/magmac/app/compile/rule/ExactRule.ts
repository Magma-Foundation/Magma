import { ? } from ?;
import { ? } from ?;
import { ? } from ?;
import { ? } from ?;
import { ? } from ?;
import { ? } from ?;
import { ? } from ?;
export class ExactRule {
	temp(){
	}
	temp(){
	}
	temp(){
	}
	temp(){
	}
}
