import { ? } from ?;
import { ? } from ?;
import { ? } from ?;
import { ? } from ?;
import { ? } from ?;
import { ? } from ?;
import { ? } from ?;
import { ? } from ?;
import { ? } from ?;
import { ? } from ?;
import { ? } from ?;
export class AfterPasser {
	temp(){
	}
	temp(){
	}
	temp(){
	}
	temp(){
	}
	temp(){
	}
}
