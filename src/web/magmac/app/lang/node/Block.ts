import { List } from "../../../../magmac/api/collect/list/List";
export class Block<H,  S> {
	Block(header : H, children : List<S>) : public {break;break;;}
	header() : H {return 0.header;;}
	segments() : List<S> {return 0.children;;}
}
