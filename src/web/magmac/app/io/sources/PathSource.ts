import { List } from "../../../../magmac/api/collect/list/List";
import { Lists } from "../../../../magmac/api/collect/list/Lists";
import { IOResult } from "../../../../magmac/app/io/IOResult";
import { Location } from "../../../../magmac/app/io/Location";
import { SafeFiles } from "../../../../magmac/app/io/SafeFiles";
import { Path } from "../../../../java/nio/file/Path";
export class PathSource {
	computeName() : String {break;break;return 0.substring( 0, 0);;}
	read() : IOResult<String> {return 0.readString( 0.child);;}
	computeLocation() : Location {return new Location( 0.computeNamespace( ), 0.computeName( ));;}
	computeNamespace() : List<String> {break;break;if(true){ return 0.empty( );;}break;break;if(true){ break;0++;;}return 0;;}
}
