import { ArrayList } from "../../../../java/util/ArrayList";
import { Arrays } from "../../../../java/util/Arrays";
export class Lists {
	of(...elements : T[]) : List<T> {return 0;;}
	empty() : List<T> {return 0;;}
	repeat(element : T, size : int) : List<T> {break;break;if(true){ break;break;;}return 0;;}
}
