import { ArrayList } from "../../../../java/util/ArrayList";
import { Arrays } from "../../../../java/util/Arrays";
export class Lists {
	of(...elements : T[]) : List<T> {return new JVMList<>( new ArrayList<>( 0.asList( 0)));;}
	empty() : List<T> {return new JVMList<>( );;}
	repeat(element : T, size : int) : List<T> {break;break;if(true){ break;0++;;}return 0;;}
}
