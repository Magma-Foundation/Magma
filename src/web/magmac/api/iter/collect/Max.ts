import { Optional } from "../../../../java/util/Optional";
export class Max {
	createInitial() : Optional<Integer> {return 0.empty( );;}
	fold(current : Optional<Integer>, element : Integer) : Optional<Integer> {return 0.of( 0.filter( 0).orElse( 0));;}
}
