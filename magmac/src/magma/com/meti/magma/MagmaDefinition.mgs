extern import { Renderable } from com.meti.node;
extern import { Optional } from java.util;

export class def MagmaDefinition() => {
	@Overridedef render() : String => {
        var valueString = value.map(inner -> " = " + inner).orElse("");
        return new MagmaDeclaration(flagString, mutabilityString, name, type).render() + valueString + ";";
    }
}