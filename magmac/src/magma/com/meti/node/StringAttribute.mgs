extern import { Optional } from java.util;

export class def StringAttribute implements Attribute() => {
	const value : String
	def StringAttribute(value : String) : public => {
        this.value = value;
    }
	@Override
	def asString() : Optional<String> => {
        return Optional.of(value);
    }
}