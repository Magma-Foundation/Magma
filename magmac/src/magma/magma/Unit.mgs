import magma.api.result.Result;
import magma.compile.CompileException;
import java.util.List;
export struct Unit {
	read : (Self) => Result<String, CompileException>;
	computeNamespace : (Self) => List<String>;
	computeName : (Self) => String;
}