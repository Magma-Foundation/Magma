import magma.api.option.None;
import magma.api.option.Option;
import magma.api.option.Some;
export default class def JSONStringParser() => public parse(input : String) : Option<JSONValue> => input.startsWith("\"")&&input.endsWith("\"")?Some(JSONString(input.substring(1, input.length()-1))):None.None();