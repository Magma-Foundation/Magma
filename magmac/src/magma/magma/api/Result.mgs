import java.util.Optional;
import java.util.function.Function;
export struct Result<T, E> {
	findValue : () => Optional<T>;
	findErr : () => Optional<E>;
	flatMapValue : ((T) => Result<R, E>) => Result<R, E>;
	mapValue : ((T) => R) => Result<R, E>;
	isOk : () => boolean;
	mapErr : ((E) => R) => Result<T, R>;
	match : ((T) => R, (E) => R) => R;
}
