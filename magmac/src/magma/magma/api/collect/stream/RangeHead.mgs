import magma.api.option.None;
import magma.api.option.Option;
import magma.api.option.Some;
export default class def RangeHead() => {
	private final extent : int;
	private counter : int;
	RangeHead(initial : int, extent : int) => {
		this=initial;
		this=extent;
	}
	public head() => {
		if
		(counter>=extent)
		return None.None();
		
		value : var=
		
		counter;
		counter++;
		return Some(value);
	}
}