import magma.api.option.Option;
import magma.api.option.Some;
() => {
	public createInitial() : Option<C> => Some(collector.createInitial());
	public fold(current : Option<C>, next : Option<T>) : Option<C> => current.and(next).map(() => collector.fold(inner.left(), inner.right()));
}