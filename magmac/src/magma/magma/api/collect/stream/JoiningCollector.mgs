import java.util.Optional;
() => {
	public createInitial() : Optional<String> => return Optional.empty();
	public fold(current : Optional<String>, next : String) : Optional<String> => return current.isEmpty()?Optional.of(next):current.map(() => inner+delimiter+next);
}