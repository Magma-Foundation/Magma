import java.util.Optional;
() => {
	public createInitial() => Optional.empty();
	public fold(current : Optional<String>, next : String) => current.isEmpty()?Optional.of(next):current.map(() => inner+delimiter+next);
}