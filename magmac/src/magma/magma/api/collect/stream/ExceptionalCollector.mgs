import magma.api.result.Ok;
import magma.api.result.Result;
() => {
	public createInitial() => Ok(collector.createInitial());
	public fold(current : Result<C, E>, next : Result<T, E>) => current.flatMapValue(() => next.mapValue(() => collector.fold(inner, inner0)));
}