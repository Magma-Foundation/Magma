import magma.api.collect.stream.Collector;
export class def ListCollector<T>(initial : List<T>) => {
	public def createInitial(this) : List<T> => {
		return initial;
	}
	public def fold(this, current : List<T>, next : T) : List<T> => {
		return current.add(next);
	}
}
