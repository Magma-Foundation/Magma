import magma.api.collect.stream.Collector;
() => {
	public createInitial() : List<T> => {
		return initial;
	}
	public fold(current : List<T>, next : T) : List<T> => {
		return current.add(next);
	}
}