import magma.api.Tuple;import magma.api.option.Option;import magma.api.collect.stream.Stream;import java.util.function.Function;public struct List {add : (T) => List<T>;popFirst : () => Option<Tuple<T, List<T>>>;default popFirstAndDiscard : Option<List<T>> => {return popFirst().map(Tuple.right);}push : (T) => List<T>;stream : () => Stream<T>;contains : (T) => boolean;isEmpty : () => boolean;last : () => Option<T>;size : () => int;mapLast : (Function<T, T>) => Option<List<T>>;}