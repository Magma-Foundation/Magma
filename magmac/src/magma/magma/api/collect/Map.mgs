import magma.api.Tuple;
import magma.api.collect.stream.Head;
import magma.api.collect.stream.Stream;
import magma.api.option.Option;
export struct Map {
	putAll : (Self, Map<K, V>) => Map<K, V>;
	get : (Self, K) => Option<V>;
	streamEntries : (Self) => Stream<Tuple<K, V>>;
	put : (Self, K, V) => Map<K, V>;
	keyStream : (Self) => Stream<K>;
	streamValues : (Self) => Stream<V>;
}