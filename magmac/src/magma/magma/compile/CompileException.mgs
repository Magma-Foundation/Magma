export class def CompileException  extends Exception() => {
	CompileException(message : String, cause : Throwable) : public => {
		super(message, cause);
	}
	CompileException(message : String) : public => {
		super(message);
	}
}
