import magma.api.result.Result;
import magma.compile.CompileParentError;
import magma.compile.Error_;
import magma.compile.rule.result.RuleResult;
export default class def ContextRule(message : String, child : Rule) => {
	public def toNode(this : Self, input : String) => child.toNode(input).mapErr(() => CompileParentError(message, input, err));
	public def fromNode(this : Self, node : Node) => child.fromNode(node);
	implements Rule;
}