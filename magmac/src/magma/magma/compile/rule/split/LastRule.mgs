import magma.compile.rule.Rule;
import magma.compile.rule.Rules;
export default class def LastRule() => {
	LastRule : public => {
		super(leftRule, slice, rightRule, () => Rules.wrapIndex(input.lastIndexOf(slice)));
	}
}