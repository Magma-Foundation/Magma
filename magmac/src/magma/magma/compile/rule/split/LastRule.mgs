import magma.compile.rule.Rule;
import magma.compile.rule.Rules;
export default class def LastRule() => LastRule(leftRule : Rule, slice : String, rightRule : Rule) => super(leftRule, slice, rightRule, () => Rules.wrapIndex(input.lastIndexOf(slice)))