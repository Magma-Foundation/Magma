import magma.compile.rule.Rule;import magma.compile.rule.Rules;import java.util.Optional;() => {FirstRule : public => {super(leftRule, slice, rightRule);}protected computeIndex : Optional<Integer> => {return Rules.wrapIndex(input.indexOf(slice));}}