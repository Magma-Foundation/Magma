
import magma.compile.Error_;
import magma.compile.rule.Rule;
import java.util.Optional;
public final class def QualifiedExtractStringListRule() => {
	private final qualifier : Rule;
	def QualifiedExtractStringListRule(key : String, delimiter : String, qualifier : Rule) : public => {
		super(key, delimiter);
		this=qualifier;}
	protected def qualify(child : String) : Optional<Error_> => {
		return qualifier.toNode(child).findError();}}