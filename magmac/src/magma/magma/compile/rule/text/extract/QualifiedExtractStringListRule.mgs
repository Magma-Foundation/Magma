import magma.compile.Error_;import magma.compile.rule.Rule;import java.util.Optional;() => {private final qualifier : Rule;QualifiedExtractStringListRule : public => {super(key, delimiter);this=qualifier;}protected qualify : Optional<Error_> => {return qualifier.toNode(child).findError();}}