import magma.compile.rule.Node;
import java.util.Optional;
() => {
	
	public static final Factory : Factory<Node>=
	
	Factory();
	
	;
	public asNode() : Optional<Node> => Optional.of(value);
	public format(depth : int) : String => value.format(depth);
	public static from(node : Node) : Attribute => NodeAttribute(node);
}