import magma.compile.rule.Node;
import java.util.Optional;
() => {
	
	public static final Factory : Factory<Node>=
	
	Factory();
	
	;
	public asNode() => Optional.of(value);
	public format(depth : int) => value.format(depth);
	public static from(node : Node) => NodeAttribute(node);
}