import magma.compile.rule.Node;
import java.util.Optional;
() => {
	
	public static final Factory : Factory<Node>=
	
	Factory();
	
	;
	public def asNode(this : Self) => Optional.of(value);
	public def format(this : Self, depth : int) => value.format(depth);
	public static def from(this : Self, node : Node) => NodeAttribute(node);
}