import java.util.List;
import java.util.Optional;
import java.util.stream.Collectors;
() => {
	
	public static final Factory : Factory<List<String>>=
	
	Factory();
	
	;
	public asStringList() : Optional<List<String>> => Optional.of(values);
	public format(depth : int) : String => values.stream().map(() => "\"" + value + "\"").collect(Collectors.joining(", ", "[", "]"));
}