import java.util.Optional;
() => {
	public asString() => Optional.of(value);
	public format(depth : int) => "\"" + escape() + "\"";
	private escape() => value.replace("\r", "\\r").replace("\n", "\\n").replace("\t", "\\t");
}