export class def CompileException extends Exception() => {	def CompileException(message : String, cause : Throwable): public => {
			super(message, Optional[Ok[value=cause]]);
	}
	def CompileException(message : String): public => {
			super(message);
	}
}
