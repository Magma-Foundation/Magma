extern import { MagmaClassNodeBuilder } from com.meti.magma;
extern import { assertCompile } from com.meti.CompiledTest;
extern import { renderJavaClass } from com.meti.java.JavaLang;

export object FeatureTest = {
	pub const TEST_SYMBOL : String = "Test";
	def assertWithinClass() : Void => {
        assertCompile(renderJavaClass(TEST_SYMBOL, input), new MagmaClassNodeBuilder().withPrefix("").withName(TEST_SYMBOL).withContent(output).build().render());
    }
}

export class def FeatureTest() => {
}