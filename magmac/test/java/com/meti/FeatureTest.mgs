extern import { assertCompile } from com.meti.CompiledTest;
extern import { renderMagmaClass } from com.meti.Compiler;
extern import { renderJavaClass } from com.meti.JavaLang;

export object FeatureTest = {
	pub const TEST_SYMBOL : String = "Test";
}

export class def FeatureTest() => {
	def assertWithinClass() : static void => {
        assertCompile(renderJavaClass(TEST_SYMBOL, input), renderMagmaClass(TEST_SYMBOL, output));
    }
}