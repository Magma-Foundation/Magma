import native stdio;

native def printf(format : Ref[I8], args : Any...) : Void;

export def print(value : String) => {
    /*
    We need to explicitly place unwrap here
    because "String" is still technically a subtype of "Any".

    - SirMathhman, 12/27/2021
    */
    printf("%s", value.unwrap());
}